#ifndef INCLUDE_SDK_COMMON_H
#define INCLUDE_SDK_COMMON_H
enum PlayType {
  PLAYREAL,
  PLAYBACK
};

enum StreamType {
  MAIN_STREAM,
  CHILD_STREAM
};

class DeviceInfo {

};

enum PlayTaskStatus {
  START,
  STOP
};
  
#endif
